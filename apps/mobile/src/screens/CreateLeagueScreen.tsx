import { SafeAreaView, Text } from "react-native";

export default function CreateLeagueScreen() {
  return (
    <SafeAreaView style={{ flex: 1, padding: 16 }}>
      <Text>Create League Screen</Text>
    </SafeAreaView>
  );
}