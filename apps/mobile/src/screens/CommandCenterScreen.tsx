import { SafeAreaView, Text } from "react-native";

export default function CommandCenterScreen() {
  return (
    <SafeAreaView style={{ flex: 1, padding: 16 }}>
      <Text>Command Center Screen</Text>
    </SafeAreaView>
  );
}