import { SafeAreaView, Text } from "react-native";

export default function MyDraftScreen() {
  return (
    <SafeAreaView style={{ flex: 1, padding: 16 }}>
      <Text>My Draft Screen</Text>
    </SafeAreaView>
  );
}